[
  {
    "name": "/reset without a name shows usage",
    "steps": [
      {
        "send": { "text": "/reset" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Usage: /reset <name>" } }
        ]
      }
    ]
  },
  {
    "name": "/reset with an unknown counter shows error",
    "steps": [
      {
        "send": { "text": "/reset testcounter" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Counter \"testcounter\" does not exist." } }
        ]
      }
    ]
  }
]